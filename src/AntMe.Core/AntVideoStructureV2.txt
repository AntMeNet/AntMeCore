[Intro]
- Hello Message Length (byte)
- "AntMe! Replay" (char[])
- Version Number (byte)

foreach Frame
	
	* FrameLength in Bytes (short)

	=== ZIPPED WITH GZIP | BEGIN ===

	* Is Keyframe (bool)

	if Keyframe

		[Header]
		* Map Width (byte)
		* Map Height (byte) 

		[Level]
		* ByteCount (short)
		* LevelFirstLoad (byte[])

		[LevelProperties]
		- PropertyType Count (byte)
		- For Count
			* PropertyName (string) [FullName]
			* ByteCount (short)
			* PropertyFirstLoad (byte[])

		[Map]
		* ByteCount (short)
		* MapFirstLoad (byte[])

		[MapProperties]
		- PropertyType Count (byte)
		- For Count
			* PropertyName (string) [FullName]
			* ByteCount (short)
			* PropertyFirstLoad (byte[])

		- for y
			- for x

				[Tiles]
				* MapTile Type Index
				if unknown tile type
					* MapTile Type Name [fullname]
				* ByteCount (short)
				* TileFirstLoad (byte[])
			
				[TileProperties]
				// TODO
	else
		[Level]
		* ByteCount (short)
		* LevelUpdateLoad (byte[])

		[LevelProperties]
		- For Count
			* ByteCount (short)
			* PropertyUpdateLoad (byte[])

		[Map]
		* ByteCount (short)
		* MapUpdateLoad (byte[])

		[MapProperties]
		- For Count
			* ByteCount (short)
			* PropertyUpdateLoad (byte[])

		- for y
			- for x

				[Tiles]
				* ByteCount (short)
				* TileUpdateLoad (byte[])
				// TODO: Ersetzbare Tiles?
			
				[TileProperties]
				// TODO
	

	foreach Faction
		[Faction]
		[FactionProperties]

	foreach Item
		[Item]
		[ItemProperties]

	=== ZIPPED WITH GZIP | END ===